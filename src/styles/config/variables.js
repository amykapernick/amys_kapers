const formatVariables = require('../../utils/formatVariables');

const variables = {
	font: {
		code: `'Source Code Pro', monospace`,
		main: 'Montserrat, sans-serif',
	}
}

module.exports = formatVariables(variables)