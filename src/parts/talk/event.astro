---
const { url } = Astro?.props;

const event = await fetch(url)
	.then((res) => res.json())
	.then((res) => res?.data?.[0]?.attributes?.title)
	.catch((err) => {
		console.log({ url, err });

		return "";
	});
---

<>{event}</>
